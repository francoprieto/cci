node {
    
    stage('Preparacion') { 
        deleteDir()
        // Clonamos el proyecto
        sh 'git clone https://github.com/DataDog/vulnerable-java-application.git'
        // Ingresamos al 
        sh 'cd vulnerable-java-application/vulnerable-java-application'
        sh 'ls -la'        
    }
    stage('Compilacion') {
        // Ingresamos al directorio del proyecto
        sh 'ls -la'
    }
    stage('Results') {
        
    }
}